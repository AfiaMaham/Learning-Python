# Find Common Elements in Two Lists Using Sets
# Write a program to find common elements in two lists.

list1 = [1, 3, 5, 7, 9]
list2 = [1, 2, 3, 4, 5]

set1 = set(list1)
set2 = set(list2)

set3 = set1.intersection(set2)

print(f"common elements are: {set3}")